package Mock;

public class Test {
	public static void main(String[] args) {
		
		//ArrayList size()
		//ArrayList add(elem)
		//ArrayList add(index,elem)
		//ArrayList increaseArraySize()
		//ArrayList get(index)
		//ArrayList remove(index)
		//ArrayList search(elem)
		//ArrayList traverse()

		// SLL add(elem)
		// SLL add(index,elem)
		// SLL size()
		// SLL get(index)
		// SLL remove()
		// SLL remove(index)
		// SLL search(elem)
		// SLL traverse()
		// SLL reserve()

		// DLL add(elem)
		// DLL add(index,elem)
		// DLL size()
		// DLL get(index)
		// DLL remove()
		// DLL remove(index)
		// DLL search(elem)
		// DLL traverse()
		// DLL reserve()

		// STACK size()
		// STACK isEmpty()
		// STACK peek()
		// STACK pop()
		// STACK push(elem)

		// QUEUE size()
		// QUEUE isEmpty()
		// QUEUE peek()
		// QUEUE poll()
		// QUEUE add(elem)

		// HashSet size()
		// HashSet isEmpty()
		// HashSet traverse()
		// HashSet add(key)
		// HashSet search(key)
		// HashSet delete(key)

		// BST add(key)
		// BST add(index,key)
		// BST search(key)
		// BST size()
		// BST remove(key)
		// BST minNode()
		// BST maxNode()
		// BST levelOrderTraverse()
		// BST preOrderTraverse()
		// BST postOrderTraverse()
		// BST inOrderTraverse()

	}
}
