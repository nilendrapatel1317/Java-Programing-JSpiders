package com.array;

public class Second_Min {
	public static void main(String[] args) {
		int[] a = {6, 3, 1, 8, 1, 4, 7, 1, 10};
		System.out.println(secondMinNum(a));
		
	}
	
	public static int secondMinNum(int[] a) {
		int firstMin = a[0];
		int secondSmall = a[0];
		
		for (int i = 0; i < a.length; i++) {
			if(a[i]==firstMin) continue;
			if(a[i] < firstMin) {
				secondSmall = firstMin;
				firstMin = a[i];
			}
			else if (a[i] < secondSmall || secondSmall == firstMin ) {
				secondSmall = a[i];
			}
		}
		return secondSmall;
	}
}
